/**
 * Tailwind v4 auto-detects content files and reads theme tokens from the
 * `@theme` block in src/app/globals.css — this file is no longer required
 * for basic setup. Kept only if you need v3-style JS-based config later
 * (e.g. custom plugins). Safe to delete.
 */
module.exports = {};
